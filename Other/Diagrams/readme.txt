Заметки.

На некоторых схемах есть классы только с именем, без описания.
Эти классы описаны в других файлах.

Свойства и события отделены от полей и методов.
Если у класса таковые имеются, события находятся между полями и методами.